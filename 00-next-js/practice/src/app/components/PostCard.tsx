// Private Folders

// 폴더이름 앞에 _를 붙이면, 해당 폴더와 그 안의 모든 파일들은 라우팅 시스템에서 완전히 제외된다.

// 폴더안에 실제 페이지가 아닌 ui컴포넌트나 비즈니스 로직 파일을 같이 두고 싶을때 사용


export default function PostCard() {
  return <h1>포스트 카드입니다~</h1>
}